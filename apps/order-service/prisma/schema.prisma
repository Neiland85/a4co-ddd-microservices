generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Order {
  id          String   @id @default(uuid())
  customerId  String
  status      String
  totalAmount Float
  currency    String   @default("EUR")
  items       Json     // Array de OrderItem serializado: [{productId, quantity, unitPrice, currency}]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([customerId])
  @@index([status])
  @@map("orders")
}
