{
  "name": "A4CO DDD Microservices DevEnv",
  "dockerComposeFile": "docker-compose.dev.yml",
  "service": "dev",
  "workspaceFolder": "/workspace",
  "shutdownAction": "stopCompose",

  // ────────────────────────────────────────────────
  // 🧩 Features preinstaladas para tu stack
  // ────────────────────────────────────────────────
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": true,
      "installOhMyZsh": true,
      "upgradePackages": true
    },
    "ghcr.io/devcontainers/features/node:2": {
      "version": "20.19.0",
      "pnpm": "latest"
    },
    "ghcr.io/devcontainers/features/python:2": {
      "version": "3.11"
    },
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },

  // ────────────────────────────────────────────────
  // 🧰 Configuración de VS Code/Cursor
  // ────────────────────────────────────────────────
  "customizations": {
    "vscode": {
      "extensions": [
        "github.copilot",
        "github.copilot-chat",
        "ms-azuretools.vscode-docker",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-kubernetes-tools.vscode-kubernetes-tools",
        "eamodio.gitlens",
        "sonarsource.sonarlint-vscode"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "files.autoSave": "afterDelay",
        "terminal.integrated.shellIntegration.enabled": true,
        "eslint.workingDirectories": ["./apps", "./packages"],
        "typescript.tsdk": "node_modules/typescript/lib",
        "workbench.colorTheme": "Default Dark+"
      }
    }
  },

  // ────────────────────────────────────────────────
  // 🔌 Puertos expuestos y servicios
  // ────────────────────────────────────────────────
  "forwardPorts": [3000, 3001, 5432, 6379, 9090, 3002],
  "portsAttributes": {
    "3000": { "label": "Frontend App" },
    "3001": { "label": "Gateway/API" },
    "5432": { "label": "Postgres DB" },
    "6379": { "label": "Redis Cache" },
    "9090": { "label": "Prometheus" },
    "3002": { "label": "Grafana" }
  },

  // ────────────────────────────────────────────────
  // 🧠 Entorno remoto y variables comunes
  // ────────────────────────────────────────────────
  "remoteEnv": {
    "DATABASE_URL": "postgresql://postgres:postgres@postgres:5432/a4co_dev",
    "REDIS_URL": "redis://redis:6379",
    "NODE_ENV": "development",
    "JWT_SECRET": "dev-secret-key",
    "JWT_ISSUER": "a4co-auth-service",
    "JWT_AUDIENCE": "a4co-platform"
  },

  // ────────────────────────────────────────────────
  // 🧪 Scripts automáticos del entorno
  // ────────────────────────────────────────────────
  "postCreateCommand": "bash /workspace/.devcontainer/init-scripts/setup.sh",
  "postStartCommand": "bash /workspace/.devcontainer/init-scripts/healthcheck.sh",

  // ────────────────────────────────────────────────
  // 👤 Usuario por defecto y CI/CD
  // ────────────────────────────────────────────────
  "remoteUser": "vscode"
}
